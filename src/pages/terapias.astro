---
import H2 from "@components/h2.astro";
import TherapyCard from "@components/therapyCard.astro";
import Contact from "@sections/contact.astro";
import { therapies } from "src/content/therapies";
import Layout from "src/layouts/Layout.astro";
---

<Layout>
  <section class="bg-neutral02 py-xxxl px-l grid gap-s">
    <H2 class="text-center"
      >Te acompaño en cada paso, sea cual sea tu situación</H2
    >
    <p class="leading-body text-center">
      Descubre las terapias disponibles y encuentra la que mejor se adapte a tus
      necesidades.
    </p>
  </section>
  <section class="px-l py-xl md:px-xxxl md:py-xxl">
    <ul class="gallery md:gap-y-[112px] gap-s">
      {
        therapies.map((card) => (
          <li class="">
            <TherapyCard {...card} />
          </li>
        ))
      }
    </ul>
  </section>
  <Contact
    title="¿Damos el primer paso?"
    text="Si tienes dudas o no sabes por dónde empezar, hablemos y encontremos juntos la mejor opción para ti."
  />
</Layout>

<style>
  .gallery {
    display: grid;
    grid-auto-rows: auto;
    grid-template-columns: repeat(auto-fill, minmax(327px, 1fr));
    justify-items: center;
    align-items: start; /* Ensures that items align to the top of the row */
  }
</style>
